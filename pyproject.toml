[project]
name = "colorwheel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.0.0",
    "sqlalchemy>=2.0.0",
    "textual>=6.11.0",
    "textual-dev>=1.8.0",
]

[dependency-groups]
dev = [
    "mbake>=1.4.3",
    "pre-commit>=4.5.0",
    "pytest>=9.0.2",
    "pytest-cov>=4.0.0",  # pytest coverage plugin
    "coverage>=7.0.0",   # coverage.py for manual coverage runs
]
