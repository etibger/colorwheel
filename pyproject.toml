[project]
name = "colorwheel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
